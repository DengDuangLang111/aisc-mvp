# 🚀 快速启动指南

> 一键启动 Study Oasis 全栈开发环境，无需手动操作！

---

## ⚡ 最快启动方式

### 方式一：一键启动（推荐）

```bash
# 从项目根目录运行
./start-servers.sh
```

**就这样！** 🎉

脚本会自动：
- ✅ 清理占用的端口 (3000, 4001)
- ✅ 启动后端 API 服务（NestJS, 端口 4001）
- ✅ 启动前端 Web 服务（Next.js, 端口 3000）
- ✅ 检查服务是否就绪
- ✅ 持续监控服务状态
- ✅ 显示日志文件位置

---

## 📋 服务器信息

启动成功后会看到：

```
🎉 所有服务器启动成功！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📡 服务器地址：
  🌐 前端: http://localhost:3000
  🔌 后端: http://localhost:4001

📝 进程 ID：
  前端: 12345
  后端: 12346

📋 日志文件：
  前端: /tmp/web.log
  后端: /tmp/api.log
```

立即打开浏览器访问：**http://localhost:3000** 🎨

---

## 🛑 停止服务

### 方式一：停止脚本

```bash
./stop-servers.sh
```

### 方式二：从启动脚本停止

按 `Ctrl+C` **一次**：停止监控脚本（服务器继续运行）  
按 `Ctrl+C` **再按一次**：完全停止

### 方式三：手动停止

```bash
# 列出正在运行的服务
ps aux | grep -E "node|next dev|pnpm" | grep -v grep

# 根据 PID 杀掉进程
kill 12345 12346

# 或者强制清理占用的端口
lsof -ti:3000 | xargs kill -9
lsof -ti:4001 | xargs kill -9
```

---

## 📊 查看日志

### 查看前端日志

```bash
# 实时查看
tail -f /tmp/web.log

# 查看最后 50 行
tail -50 /tmp/web.log

# 清空日志
> /tmp/web.log
```

### 查看后端日志

```bash
# 实时查看
tail -f /tmp/api.log

# 查看最后 50 行
tail -50 /tmp/api.log

# 清空日志
> /tmp/api.log
```

---

## 🔧 常见问题

### Q: 启动脚本没有权限？

```bash
# 添加执行权限
chmod +x start-servers.sh stop-servers.sh

# 然后重新运行
./start-servers.sh
```

### Q: 端口已被占用？

脚本会自动清理，但如果还是有问题：

```bash
# 手动清理
lsof -ti:3000,4001 | xargs kill -9

# 然后重新运行
./start-servers.sh
```

### Q: 服务启动超时？

检查日志：

```bash
# 查看前端
tail -50 /tmp/web.log

# 查看后端
tail -50 /tmp/api.log

# 确保依赖已安装
pnpm install
```

### Q: 修改代码后需要重启吗？

**不需要！** 两个服务都支持热重载 (HMR)：

- **前端**：修改代码立即更新（无需刷新页面）
- **后端**：修改代码立即重启（无需手动操作）

### Q: 如何进行完整的全新启动？

```bash
# 完整重启步骤
./stop-servers.sh          # 1. 停止服务
rm -rf .next              # 2. 清除前端缓存
./start-servers.sh         # 3. 重新启动
```

---

## 🧪 快速测试

### 测试前端

```bash
# 在浏览器中打开
http://localhost:3000

# 或者 curl 测试
curl http://localhost:3000
```

### 测试后端

```bash
# 健康检查
curl http://localhost:4001/health

# 查看 Swagger API 文档
curl http://localhost:4001/api/docs
```

### 完整系统测试

```bash
# 项目根目录运行
./verify-system.sh
```

---

## 💡 高级用法

### 只启动后端

```bash
cd apps/api
PORT=4001 pnpm start:dev
```

### 只启动前端

```bash
cd apps/web
PORT=3000 pnpm dev
```

### 在不同终端运行

```bash
# 终端 1：启动后端
cd apps/api && PORT=4001 pnpm start:dev

# 终端 2：启动前端
cd apps/web && PORT=3000 pnpm dev
```

### 使用 tmux 或 screen 后台运行

```bash
# 使用 tmux（更强大）
tmux new-session -d -s study-oasis './start-servers.sh'

# 查看运行状态
tmux attach-session -t study-oasis

# 退出 tmux（Ctrl+B, 然后 D）
```

---

## 🎯 开发工作流

### 典型的开发流程

```bash
# 1️⃣ 启动服务
./start-servers.sh

# 2️⃣ 打开浏览器
# 在浏览器中访问 http://localhost:3000

# 3️⃣ 修改代码
# 编辑 apps/web 或 apps/api 中的文件

# 4️⃣ 自动热更新
# 前端：页面自动刷新
# 后端：服务自动重启

# 5️⃣ 测试功能
# 在浏览器中测试新功能

# 6️⃣ 提交代码
git add .
git commit -m "feat: 功能描述"
git push origin main

# 7️⃣ 停止服务
./stop-servers.sh
```

---

## 📱 浏览器访问

### 前端应用
- **地址**: http://localhost:3000
- **功能**: Chat, 文件上传, 对话历史

### 后端 API
- **Health**: http://localhost:4001/health
- **Swagger Docs**: http://localhost:4001/api/docs
- **GraphQL**: http://localhost:4001/graphql (如果启用)

---

## 🔍 监控和调试

### 实时监控所有进程

```bash
# 显示所有 Node 进程
ps aux | grep -E "node|next|pnpm" | grep -v grep

# 或使用 lsof 查看端口占用
lsof -i :3000
lsof -i :4001
```

### VS Code 集成调试

1. 安装 **Rest Client** 扩展
2. 使用 `.http` 文件测试 API
3. 设置断点并使用调试器

```json
// .vscode/launch.json
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Attach NestJS",
      "port": 9229,
      "restart": true
    }
  ]
}
```

---

## 📞 获取帮助

### 查看系统状态

```bash
./verify-system.sh
```

### 查看脚本源代码

```bash
cat start-servers.sh
cat stop-servers.sh
```

### 查看项目文档

```bash
# 查看完整的开发指南
cat docs/DEVELOPMENT_GUIDE.md

# 查看 API 文档
cat docs/API_DOCS.md

# 查看架构设计
cat docs/ARCHITECTURE.md
```

---

## ✨ 下次记住这个命令就行

```bash
./start-servers.sh
```

就这一个！🎉

不需要记住其他复杂的命令，一切都会自动处理。

---

**祝开发愉快！** 🚀
